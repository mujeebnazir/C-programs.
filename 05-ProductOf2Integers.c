//program to multiply to two integers.
int main()
{
	int a,b,c;
	printf("Enter two integers you want to multiply: ");
	scanf("%d %d",&a,&b);
	c= a*b;
	printf("%d multiplyed by %d is %d",a,b,c);
	return 0;
}
